<!DOCTYPE html>
<html lang="en">

<head >
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title >FurFile</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.4/css/bulma.min.css">
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma-carousel@4.0.3/dist/css/bulma-carousel.min.css">
</head>

<body class = "background">
  <header  >
    <h1 class ="header">FurFile</h1>
    <nav class="navbar" role="navigation" aria-label="main navigation">
      <div class="navbar-brand">
        <img class="navbar-item" src="assets\furfilepaw.png">
      </div>
        <div class="navbar-menu">
      </div>
        <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
      <!-- Conditionally render login or logout links -->
      {{#if logged_in}}
      <a class="button is-light" href="/">Homepage</a>
      <a class="button is-light" href="/user-profile">User Profile</a>
      <button class="button is-light" id="logout">Logout</button>
      {{else}}
      <a class="button is-light" href="/">Homepage</a> 
      <a class="button is-light" href="/user-profile">User Profile</a> 
      <a class="button is-light" href="/login">Login</a>
      {{/if}}
    </nav>
  </header>
  <main>
    {{{body}}}
  </main>
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  
  {{/if}}
</body>

</html>