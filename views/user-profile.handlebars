    <div class="container mt-6">
        <div class="profile-info">
            <h2 class="title is-4 has-text-centered">{{name}}
            <p>Email: {{email}}</p>
            </h2>
        </div>
        <h3 class="is-5 has-text-left">Pets</h3>

        {{#each Pets}}
            <div class="pet-card">
                <div class="pet-icon"></div> <!-- Placeholder for pet icon -->
                <div class="pet-details">
                    <p class="pet-name">{{name}}</p>
                </div>
                <a href="/pets/{{id}}"> <!-- Link to pet profile -->
                    <span class="arrow-icon">➔</span> <!-- Arrow icon -->
                </a>
            </div>
        {{/each}}
        </ul>
        <br>
        <br>
        <br>
        {{> addPetForm}}
    </div>

     <script src="/js/petform.js"></script>
